version: 1
frontend:
  phases:
    build:
      commands:
        - npm install  # Install dependencies
        - npm run build # Build the React application
  artifacts:
    baseDirectory: build # The directory where the built files are placed
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*' # Cache your node_modules directory